<%= form_with(model: dianosaur, local: true) do |form| %>
  <% if dianosaur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dianosaur.errors.count, "error") %> prohibited this dianosaur from being saved:</h2>

      <ul>
      <% dianosaur.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dianosaur_name %>
  </div>

  <div class="field">
    <%= form.label :genus %>
    <%= form.text_field :genus, id: :dianosaur_genus %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
